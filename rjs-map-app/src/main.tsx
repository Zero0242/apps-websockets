import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'
import { MapasApp } from './MapasApp.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MapasApp />
  </React.StrictMode>,
)
